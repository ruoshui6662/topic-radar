# 微信公众号选题工作台 — 技术规划 v0.4

> 状态：**方向已定 + PM 审查已过（2026-08-16），可开工**
> 原则：第一性原理拆解 —— 采集 → 量化 → 评估 → 生成/分发，四层解耦，地基（采集层）优先验证

---

## 1. 项目定位

为公众号运营者提供**每日自动化的选题决策系统**：

- 自动监测全网热点 + 目标公众号内容流，量化话题热度（阅读量/点赞/分享/发文账号数 → 代理指标）
- 结合账号画像评定选题价值，划分 S/A/B/C 等级
- 产出：选题卡片（热度数据、价值理由、竞争度、时效窗口）+ 写作方向 + 写作建议 + 标题候选
- 形态：每日定时推送 + Web 工作台 + 随时提问的对话式查询

## 2. 已确认的方向决策

| 维度 | 决策 | 说明 |
|---|---|---|
| 微信数据源 | 免费内容流 + 代理指标 | wewe-rss 等自建，不付费数据商 |
| 部署 | NAS ≥8G 内存，Docker Compose | 全量方案：Postgres + SearXNG + wewe-rss + tophub + app |
| 产品形态 | 每日推送 + 随时提问 + Web UI | 推送为日报；对话为按需查询；Web 为工作台本体 |
| 技术栈 | Python 为主 | FastAPI + 轻前端，单用户可维护 |
| LLM | DeepSeek API（OpenAI 兼容） | 统一封装可换；embedding 本地 bge-small-zh |
| 内容领域 | 科技 / AI 类 | 热榜权重、画像模板、初始账号清单按此定制 |
| 推送渠道 | 飞书机器人 | adapter 可插拔 |
| 网络访问 | 仅内网访问 | 后续外网需求用 Tailscale 扩展 |
| **热榜采集** | **tophub 开源容器（主）+ 自写 adapter 补缺** | 200+ 源开箱即用，省 3~5 天开发量，抗接口变动 |
| **wewe-rss 验证** | **M1 第 1 周 spike，M1 末 Go/No-Go 决策** | 最大外部风险最早暴露 |
| **代理指标校准** | **外部基准校验（每周 10 分钟人工抽查录入）** | 免费路线下唯一靠谱的标定手段 |
| 时间投入假设 | 每周 5~8 小时 | 总周期 7~8 周（非文档里 2+3+2 的理想值） |

## 3. PM 审查修订记录（v0.3 → v0.4）

| 级别 | 问题 | 修订 | 状态 |
|---|---|---|---|
| P0-1 | 「按阅读量定热度」免费方案做不到，验收预期不齐 | 明确「热度 = 跨平台热度 + 发文密度代理」并以此为验收口径；增加外部基准校验 | ✅ 已采纳 |
| P0-2 | 自己账号样本太少，自动校准数学上不成立 | 权重改为「先验 + 可视化人工微调」；自动校准定位为辅助 | ✅ 已采纳 |
| P0-3 | wewe-rss 波动/合规风险无触发点和决策点 | 周 1 spike + 独立微信小号 + M1 末 Go/No-Go 决策门 | ✅ 已采纳 |
| P1-1 | 自写 7 平台采集器是纯体力活 | 改用 tophub 容器，只补缺失源 | ✅ 已采纳 |
| P1-2 | 基础设施排在 LLM 日报质量验证之前 | 周 1 先手工搓 2~3 份样例日报共创格式，再自动化 | ✅ 已采纳 |
| P1-3 | 验收标准不可测试 | 全部改为量化验收（§13） | ✅ 已采纳 |
| P1-4 | 缺 KPI 与决策门 | 每阶段 Go/No-Go 决策门 + KPI 指标（§13） | ✅ 已采纳 |
| P1-5 | M3 前端 8 页面范围偏大 | 裁剪为两梯队：MVP 三页（今日选题/对话/选题库）+ 第二梯队 | ✅ 已采纳 |
| P1-6 | 缺样例选题卡，产品形态未钉死 | 新增 §16 附录样例选题卡 | ✅ 已采纳 |
| P1-7 | 运维细节缺失 | 补 restart 策略/时区/备份恢复演练/飞书消息限制 | ✅ 已采纳 |
| P1-8 | 指标源未抽象 | 指标来源 provider 接口抽象，代理 ↔ 付费可切换 | ✅ 已采纳 |

## 4. 总体架构

```
┌────────────────────────────────────────────────────────────┐
│                    Docker Compose (NAS)                     │
│                                                            │
│  ┌────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────┐ │
│  │ tophub │ │ searxng  │ │ wewe-rss │ │ postgres │ │ app │ │
│  │ 热榜聚合│ │ 搜索聚合  │ │ 公众号RSS │ │   主库   │ │Fast │ │
│  └───┬────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘ │API  │ │
│      │           │            │            │        │Sched│ │
│      └───────────┴────────────┴────────────┴────────┴─────┘ │
│                         ▲  app 定时采集入库                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           web (vue3 + echarts)                       │   │
│  │   今日选题 / 话题热榜 / 选题库 / 对话 / 复盘 / 画像    │   │
│  └─────────────────────────────────────────────────────┘   │
│                        ▲         │                          │
│             飞书机器人（日报）      每日定时(APScheduler)      │
└────────────────────────────────────────────────────────────┘
```

### 四层职责

| 层 | 模块 | 职责 | 关键点 |
|---|---|---|---|
| 采集层 | tophub、SearXNG、wewe-rss、行业 RSS | 拿原始信号 | 多源冗余，单源挂了不影响整体 |
| 量化层 | 清洗去重、话题聚类、热度评分 | 信号 → 可比较的数字 | 代理指标体系 + 外部基准校验（§5） |
| 评估层 | 账号画像 + LLM 选题评估 | 结合账号特性评价值 | 权重先验 + 人工微调（§6） |
| 生成分发层 | 日报生成、对话问答、Web API | 输出选题卡/建议/标题 | 日报 + 按需对话双通道 |

## 5. 数据源与采集方案

### 5.1 热榜采集（tophub 容器为主）

- **tophub（今日热榜，开源 Docker 容器）**：微博热搜、知乎热榜、百度、抖音、Hacker News、GitHub Trending、V2EX、IT之家 等 200+ 源开箱即用
- app 定时（30 min）从 tophub 拉取 → 存位次轨迹 → 形成**热度趋势曲线**
- 仅对 tophub 缺失且必要的源自写 adapter（如搜一搜热点，待实测，备选）
- 接口变动由 tophub 社区维护吸收，本系统零维护成本

### 5.2 SearXNG（联网搜索）

- 话题扩展：热榜词 → 搜索关联新闻，补充话题上下文（喂给 LLM）
- 选题佐证：对候选选题搜索最新动态，评估时效窗口
- 备用热点源（定期搜行业词如「AI 大模型」「智能体」）

### 5.3 行业信源 RSS（科技/AI 定制）

IT之家、36氪、少数派等 RSS 入库；机器之心/量子位等无公开 RSS 的由 wewe-rss 覆盖。

### 5.4 微信公众号内容流（核心难点）

**硬事实**：公众号无公开搜索/数据 API；阅读量、点赞、在看只在微信客户端内加载（`getappmsgext`），浏览器不可见；搜狗微信反爬极严。→ **内容流免费可得，指标流（别人阅读量）基本只能付费，本方案用代理指标近似。**

**内容流选型：wewe-rss（首选）**
- 原理：借微信读书账号订阅公众号 → 导出 RSS（标题/正文/链接/发布时间）
- 覆盖：手动维护的自选账号清单（建议 50~200 个）
- **合规红线：必须使用独立的微信小号**（微信读书凭证绑定个人微信），绝不绑日常主号
- **验证计划：M1 第 1 周 spike（3 天）**——部署 wewe-rss，实际订阅 3 个号跑通 RSS，M1 末做 Go/No-Go 决策
- 预案：A 微信协议机器人（gewechat）；B RSSHub 微信路由；C 搜狗手动补充——**预案在 M1 spike 时同步验证**，不等断供那天
- 抓取频率 1~2h 一次，降低账号风险

**初始账号清单建议（科技/AI，20 个起步）**：

| 类别 | 账号 |
|---|---|
| AI 前沿 | 量子位、机器之心、新智元、AI前线 |
| 科技商业 | 36氪、虎嗅APP、晚点LatePost、半佛仙人 |
| 数码产品 | 爱范儿、差评、ZEALER |
| 效率工具 | 少数派、AppSo |
| 开发者社区 | InfoQ、CSDN、程序员的那些事 |
| 互联网观察 | 阑夕、caoz的梦呓、刘润 |

**自己账号的数据**：公众平台后台可导出自己文章 Excel（阅读量/分享/涨粉），定期导入回填。

## 6. 数据处理与热度量化

### 6.1 流程

```
原始信号 → 清洗(去HTML/去广告/标题归一化) → 去重(标题hash + embedding相似度)
        → 话题聚类(关键词 + embedding聚类，HDBSCAN)
        → 话题画像(关键词集、首发时间、发文账号集合、各平台热度轨迹)
        → 热度评分 → 入库 topics / topic_articles
```

- embedding：本地 bge-small-zh（CPU 可跑，NAS ≥8G 无压力）
- 聚类粒度：同一话题跨平台聚合（"某模型发布"在微博/知乎/HN 是同一个话题）

### 6.2 代理指标体系

**重要口径（验收以此为准）**：本系统的「热度」= 跨平台热度 + 公众号发文密度代理，**不是真实阅读量**。二者相关性由下面的基准校验机制保证。

| 指标 | 数据来源 | 权重（先验） | 说明 |
|---|---|---|---|
| 跨平台热榜位次与持续时间 | tophub 轨迹 | 高 | 热点跨平台传导，微信内话题通常先在外围爆 |
| 发文账号数量 | 公众号内容流 | 高 | 直接对应需求「发文账号数量」 |
| 发文速率（短时间密集度） | 公众号内容流 | 高 | 1h 内多篇 = 正在发酵 |
| 头部账号参与度 | 公众号内容流 | 中 | 关注的头部号是否下场 |
| 话题新鲜度/时间衰减 | 系统时间 | 中 | 指数衰减，12h 半衰期起调 |
| 搜索热度佐证 | SearXNG 结果量 | 低 | 补充验证 |

```
话题热度 = Σ(wᵢ × 归一化指标ᵢ) × 时间衰减因子
```

### 6.3 外部基准校验（代理指标的标定）

- **每周 10 分钟**：用新榜免费榜页/壹伴抽查 5~10 篇相关文章的真实阅读量/点赞，人工录入系统（calibration 表）
- 用途：① 校正代理权重（先验 → 有依据的修正）；② 生成「代理分 vs 真实数据」对照，判断代理体系是否仍可靠
- 长期：样本积累后，可自动拟合各指标权重（辅助，不承诺）

### 6.4 话题热度等级

- S：全平台爆 + 账号密集发文，窗口 12~24h
- A：多平台热 + 有发文，窗口 24~72h
- B：单平台热或行业热点，窗口 3~7 天
- C：长尾/趋势型，可备选

## 7. 选题评估与生成（LLM 层）

### 7.1 账号画像（用户维护，可视化编辑）

```yaml
# 示例（科技/AI 默认模板，待用户填写）
领域: [AI工具, 效率办公]        # 精确到你的细分
人设: 实用主义测评号，语言直白，带数据
目标读者: 25-40岁职场人
优势: 深度评测、实操教程
禁忌: 无数据支撑的夸张结论
历史爆款模式: [教程类标题带数字，测评带对比]
历史数据: 近期10篇文章（阅读/分享/涨粉）← 后台导出回填
```

### 7.2 权重与评分的调整机制（P0-2 修订）

- 权重 = **先验值（经验定）+ 人工微调**：工作台提供可视化拖拽/滑块直接调各指标权重，改完立即重算历史话题检验效果
- 外部基准校验数据自动生成调整建议（每周一条：「建议把发文速率权重从 0.3 提到 0.35，依据：最近 12 篇抽查相关性 0.72」）
- **不承诺自动校准**：它只是辅助，避免样本不足导致过拟合

### 7.3 选题评估（每日对候选话题跑一遍）

LLM（function calling / 结构化输出）输入：话题卡 + 画像 + 竞争信息 → 输出选题卡（格式见 §16 附录）：

```
选题卡 = {
  话题, 热度等级, 价值评分(0-100),
  匹配理由, 竞争度, 时效窗口,
  建议等级: S/A/B/C,
  写作方向: 2~3个切入角度,
  写作建议: 结构/节奏/数据点,
  标题候选: 3~5个(含风格变体)
}
```

- LLM：DeepSeek；等级判定 = LLM 输出 + 规则硬约束（如：账号 0 发文的热点降级）
- 失败降级：LLM 调用失败 → 保留规则评分，日报照常出

### 7.4 反馈闭环

发布文章 → 回填数据 → 复盘对比「当时评分 vs 实际表现」→ 提示画像/权重调整建议（辅助定位，见 §7.2）。

## 8. 随时提问（对话式查询）

- 入口：Web 工作台对话窗
- 实现：LLM function calling + 检索（不搞重 RAG）：

```
用户问题 → 意图识别(工具选择) →
  query_hot_topics(今日S/A级选题) /
  query_topic(topic, 热度趋势/发文账号) /
  suggest_titles(话题, 画像) /
  review_period(近7天复盘) /
  profile_advice(画像优化建议)
→ 检索数据库 → LLM 组装回答
```

- 示例："今天有什么值得写的？"、"DeepSeek 出新模型这个话题现在热度怎么样？哪些号发了？"、"给个关于 AI 编程工具的 10 万加标题"、"上周我发的选题表现如何？"
- 原则：LLM 只做理解+组织，数据永远来自库

## 9. Web 工作台

- 前端：Vue3 + Vite + Element Plus + ECharts
- **MVP 三页（M3 第一梯队）**：

| 页面 | 内容 | 生动展示元素 |
|---|---|---|
| 今日选题 | 选题卡片流（S/A/B/C 标签、价值分、理由、写作方向、标题候选、一键采纳） | 卡片分级配色、热度雷达图 |
| 对话 | 随时提问入口 | 对话流 + 富卡片回答 |
| 选题库 | 历史选题：搜索/筛选/收藏、状态流转（未采纳/已采纳/已发布） | 看板视图 |

- **第二梯队（M3 后半/持续迭代）**：话题热榜（趋势折线/词云）、话题详情（发文账号列表、多平台轨迹）、账号画像（可视化编辑）、复盘（评分-表现散点图、命中率）、系统状态（源健康/任务日志/失败计数）
- 说明：ECharts「生动展示」是加分项，不是 M3 验收项

## 10. 技术栈清单

| 层 | 选型 | 理由 |
|---|---|---|
| 编排 | Docker Compose | NAS 标准姿势，一条命令起全部 |
| 后端 | Python 3.11+ / FastAPI | 生态全、异步、自带 OpenAPI 文档 |
| 调度 | APScheduler（进程内 cron） | 单用户规模足够 |
| 数据库 | Postgres 16 | 关系数据 + 后续 pgvector 扩展空间 |
| 热榜采集 | tophub 容器（开源） | 200+ 源免维护；缺失源自写 httpx adapter |
| 公众号流 | wewe-rss（独立容器，输出 RSS） | feedparser 解析 |
| 搜索 | SearXNG 自建 | 隐私 + 可配置引擎 |
| LLM | DeepSeek API（OpenAI 兼容，统一封装可换） | 便宜、中文强 |
| Embedding | bge-small-zh 本地 | 零 API 成本 |
| 前端 | Vue3 + Element Plus + ECharts | 生态成熟、图表强 |
| 推送 | 飞书机器人 | adapter 可插拔（预留企业微信） |
| **指标源抽象** | **provider 接口：代理指标 ↔ 付费数据商可切换** | 后期想上真实阅读量不动架构 |

## 11. 数据模型（草案）

```sql
accounts      (id, name, biz_id, category, level, follow_count_est, status)
articles      (id, account_id, title, url, summary, content_hash, publish_time, source)
topics        (id, title, keywords[], first_seen_at, heat_score, heat_level, status)
topic_articles(topic_id, article_id, role)              -- 话题-文章关联
hot_items     (id, platform, rank, keyword, heat, captured_at)  -- 热榜轨迹(tophub)
selections    (id, topic_id, grade, value_score, reason, direction, advice, titles[], status, created_at)
profile       (id, content_json, version)               -- 账号画像
feedback      (id, selection_id, article_id, reads, shares, fans_delta, published_at)
calibration   (id, topic_id, checked_at, real_reads, real_likes, real_shares, note)  -- 外部基准抽查
chat_logs     (id, question, answer, tool_used, created_at)
```

## 12. 部署方案（NAS）

- 前置：Docker（群晖 Container Manager / 威联通 Container Station / UNRAID），≥8G 内存已确认
- 目录：`/docker/wechat-workbench/{app,searxng,wewe-rss,tophub,pgdata,logs}`
- 资源预估：Postgres ~0.3G + SearXNG ~0.5G + wewe-rss ~0.3G + tophub ~0.2G + app ~0.3G + 前端静态页 ≈ **1.5~2G 内存**
- 访问：局域网 IP:端口（仅内网）；后续外网用 Tailscale
- **运维规范**：
  - 所有容器 `restart: unless-stopped`（NAS 重启自启）
  - 全局 `TZ=Asia/Shanghai`（定时任务依赖）
  - Postgres 数据卷挂 NAS 快照目录，**每季度做一次恢复演练**（不演练=没备份）
  - 飞书消息长度限制：日报精简为摘要 + 关键选题卡 + Web 工作台链接，详情网页看

## 13. 调度与可靠性

| 任务 | 频率 | 说明 |
|---|---|---|
| 热榜采集（tophub → 入库） | 每 30 min | 存位次轨迹 |
| 公众号内容流同步 | 每 1~2 h | wewe-rss 拉取 → 入库去重 |
| SearXNG 话题扩展 | 每日 2 次 | 活跃话题补充新闻素材 |
| 话题聚类 + 热度评分 | 每 2 h | 增量计算 |
| 选题评估（LLM） | 每日 7:30 | 生成选题卡 |
| 日报推送 | 每日 8:00（可调） | 飞书推送 S/A 级选题 |
| 复盘统计 | 每日 8:00 | 回填数据对比 |

可靠性：单平台失败自动跳过并告警；LLM 失败重试 + 规则降级；工作台「系统状态」页展示任务日志与失败计数。

## 14. 分期实施计划（含决策门与验收标准）

### 总预算：每周 5~8 小时，7~8 周

| 阶段 | 内容 | 周期 | 量化验收标准 |
|---|---|---|---|
| **M1 热点雷达** | 周 1：docker 基建（本机先跑同一套 compose）+ **wewe-rss spike（3 天，Go/No-Go）** + 手工搓 2~3 份样例日报与用户共创格式；周 2：tophub 接入 + 话题聚类 v1（关键词级）+ 热度评分 + LLM 日报自动化 + 飞书推送 | ~2 周 | ① 热榜采集连续 7 天无中断；② 每日话题 ≥30 个；③ 日报连续 3 天无人工干预自动发出；④ 用户对 S/A 级选题与自身直觉一致性 ≥70%；⑤ wewe-rss spike 出结论（Go/No-Go） |
| **M2 内容流** | wewe-rss 正式接入 + 账号清单 + 文章入库 + 发文账号数/速率指标 + 话题-文章关联 + **外部基准校验机制上线（每周抽查）** + 评分升级 | ~3 周 | ① 选题卡含公众号侧数据（谁发了/几篇/速率）；② 抽查录入流程跑通 2 轮；③ 代理分与抽查真实阅读量的排序一致性 ≥60%（持续爬升） |
| **M3 工作台** | MVP 三页（今日选题/对话/选题库）+ 画像可视化编辑 + 复盘 + 反馈闭环 | ~2 周 | ① 网页完成浏览/采纳/收藏全流程；② 对话问出的数据与库一致；③ 发布→回填→复盘闭环跑通 1 次 |
| **M4 打磨** | 权重可视化微调、推送模板优化、稳定性加固、第二梯队页面 | 持续 | 连续 30 天无人工干预稳定运行 |

### KPI（每周复盘用）

1. 采纳率：每周从工作台采纳的选题数（目标 ≥3）
2. 命中率：采纳选题文章表现 vs 同期未采纳文章（阅读中位数对比，目标采纳 > 未采纳）
3. 效率：日报阅读耗时 <5 分钟
4. 一致性：系统分级 vs 用户直觉一致率 ≥70%

### Go/No-Go 决策门

- **Gate 1（M1 末）**：日报可用性 + 一致性达标 + wewe-rss 结论明确 → 进 M2；不达标 → 先修 M1 再走
- **Gate 2（M2 末）**：内容流指标生效 + 基准校验跑通 → 进 M3
- **Gate 3（M3 末）**：闭环跑通（采纳→发布→回填→复盘）→ 进 M4 打磨

### 开工前置清单（用户准备）

1. DeepSeek API key（platform.deepseek.com 注册充值）
2. 飞书群 + 自定义机器人 webhook（建群 → 机器人 → 复制 webhook）
3. **独立微信小号**（专用于微信读书订阅，供 wewe-rss 使用）
4. NAS 确认 Docker 就绪 + 规划存储目录
5. 本机（Windows）装 Docker Desktop 用于开发，**开发与部署用同一套 docker-compose**，迁移只改路径

## 15. 风险与预案

| 风险 | 影响 | 预案 |
|---|---|---|
| wewe-rss 失效/波动 | 内容流断供 | **周 1 spike 前置验证**；预案 A 微信协议机器人；B RSSHub；C 手动导入 |
| 微信小号被限制 | 内容流断供 | 独立小号隔离风险；低频抓取；多号轮换 |
| 代理指标与真实阅读量偏差 | 分级不准 | 外部基准校验每周标定；权重可视化微调；口径已在文档对齐 |
| tophub 源变动/下线 | 单平台数据缺失 | 社区维护吸收；缺失源自写 adapter 兜底 |
| NAS 资源不足 | 容器 OOM | 8G 确认充裕；降级预案（SQLite + 限内存） |
| LLM API 成本/波动 | 日报失败 | DeepSeek 低价 + 重试 + 规则降级 |
| 用户每周 10 分钟抽查坚持不了 | 标定失效 | 校验做成可选开关；缺数据时自动降级为先验权重并提示 |

## 16. 附录：样例选题卡（产品形态钉死用）

> 状态更新（2026-08-17）：**M1 日报模板已定稿**——S 级详细卡 + A 级精简卡 + B/C 隐藏；标题采用"10万+"风格（悬念/冲突感/具体数字）；生成方式为分块并行 LLM 评估（默认 `ocg/glm-5.2`，12 话题约 47 秒）。

```
┌─ 今日选题卡 · S1 ─────────────────────────────────────┐
│ 话题：DeepSeek 发布新模型 X（首发 2026-08-16 09:12）    │
│ 热度：92/100 · S 级 · 时效窗口 24~48h                  │
│ 跨平台：微博热搜 #3（持续 3h） 知乎热榜 #1（5h） HN #1   │
│ 公众号侧：6 个头部号已发（量子位/机器之心/新智元…）       │
│           首篇 09:12，最新 11:30，速率 1.5 篇/h         │
│ 价值理由：与账号领域高度相关；头部号已下场但尚无深度实测   │
│ 竞争度：高（6 号已发）→ 需差异化角度                    │
│ 写作方向：① 实测跑分+上手体验 ② 对普通用户的实际影响      │
│           ③ 与前代模型对比                            │
│ 写作建议：前 300 字给结论；至少 3 个数据点；结尾引导在看  │
│ 标题候选：                                            │
│   1.「DeepSeek X 实测：3 个场景跑分颠覆我的预期」        │
│   2.「别急着升级！DeepSeek X 这几个坑先说清楚」          │
│   3.「DeepSeek X vs 前代：差在哪 5 个数字告诉你」        │
└───────────────────────────────────────────────────────┘
```

## 17. 待确认问题清单（不阻塞开工）

1. **初始账号清单**：§5.4 的 20 个账号是否符合竞品范围？M2 前确认增删即可
2. **日报推送时间**：默认 8:00，开工后可调
3. **画像细节**：公众号细分领域、人设、目标读者（§7.1），M3 前填写即可

> **当前状态：方向已定，PM 审查通过，决策门就位，可以启动 M1。**
